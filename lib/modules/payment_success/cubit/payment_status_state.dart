import '../../../utils/exports.dart';

/// Immutable state for the payment success screen.
class PaymentStatusState extends BaseState {
  /// Creates an instance of [PaymentStatusState].
  const PaymentStatusState({
    super.redirectRoute,
    required super.status,
  });
}
